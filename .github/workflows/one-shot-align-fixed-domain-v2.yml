name: One-shot align fixed-domain hosting v2

on:
  push:
    branches:
      - chatgpt/fixed-domain-hosting

permissions:
  contents: write

jobs:
  patch-test-commit:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          fetch-depth: 0
          persist-credentials: true
      - run: python .github/scripts/align-fixed-domain.py
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6.5.0
        with:
          node-version: 24.18.0
          cache: npm
      - run: npm ci --ignore-scripts --audit=false
      - run: npm run test:all
      - name: Commit tested alignment and remove one-shot files
        shell: bash
        run: |
          rm .github/workflows/one-shot-align-fixed-domain.yml
          rm .github/workflows/one-shot-align-fixed-domain-v2.yml
          rm .github/scripts/align-fixed-domain.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: keep fixed-domain hosting cross-platform"
          git push origin HEAD:chatgpt/fixed-domain-hosting
